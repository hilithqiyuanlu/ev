好，这次把这一串概念从头到尾讲明白。核心就两个概念：**响度（RMS）**和**信噪比（SNR）**，其它都是为它们服务的。

## 1. dB 到底是什么

所有音量、噪声、SNR 都挂在 dB 这个单位上。它是个**对数刻度**，用来表达"一个数比另一个数大多少倍"：

- **+3dB = 功率 ×2**（或"感觉大了一倍"）
- **+6dB ≈ 幅度 ×2**
- **+20dB = 功率 ×100**（感觉大 10 倍）
- **+60dB = 功率 ×100 万**

为什么要用对数？因为人能听到的声音范围是"小声到巨响"跨越百万倍，线性数字没法用，对数把"倍数"压成了好读的数字。**dB 永远是相对值**——相对某个基准。

## 2. rawRMS —— "这段声音有多响"

RMS（均方根）是把一段音频的**平均音量**算成一个数字：每个采样值平方→求平均→开根号。数值 0.0~1.0，越大越响。

- 0.0005 ≈ -66dBFS：贴着地板几乎听不见
- 0.01 ≈ -40dBFS：正常说话
- 0.1 ≈ -20dBFS：很大声了

**为什么必须是 raw 的**：你的信号经过 AGC（自动音量）后，小声也会被拉到标准响度，processed 后的 RMS 恒等于目标值（0.08 左右），**等于什么信息都没有**。只有 AGC 之前的原始信号才说真话。这就是"rawRMS 污染"那件事。

## 3. 噪声底噪（noise floor）—— "这个房间平时有多吵"

你录音时，房间里永远有背景声：空调嗡嗡、马路、电流底噪。**底噪 = 没人说话时房间的背景响度**。它不是固定的——深夜和开着空调时完全不同。

`RawNoiseTracker` 干的事就是**一直测量这个底噪**，只在"确实安静时"更新，说话时冻结（不然你的呼吸停顿会把底噪拉虚）。

## 4. SNR —— "这段声音比房间底噪高出多少"

**SNR（信噪比）= 信号响度 ÷ 底噪响度**，再换成 dB：

```
SNR(dB) = 20 × log10( 说话声RMS / 底噪RMS )
```

例子：底噪 RMS=0.001（-60dBFS），说话 RMS=0.01（-40dBFS）
→ 0.01/0.001 = 10 → 20×log10(10) = **20dB**

**为什么用 SNR 而不用绝对响度？** 因为可懂度取决于"和背景的对比度"。同样的说话音量，在安静房间很清楚，在嘈杂房间听不清——**绝对响度没变，SNR 变了**。SNR 才是"人耳能不能听清"的度量。这就像：在图书馆喊一声 vs 在菜市场喊一声。

## 5. 颜色分型为什么是 10 / 3

因为这两个数对应**人类听感的真实分界**（"第二人耳"就是按这个设计的）：

| 颜色 | SNR | 人耳感受 | 转写状态 |
|------|-----|---------|---------|
| 绿 | ≥10dB | 清楚、不费力 | 正常转写，放心 |
| 橙 | 3~10dB | 能听见但费劲，要集中注意 | 转写开始出错，警惕 |
| 红 | <3dB | 埋在噪声里，基本听不清 | 不值得转写 |

- **10dB 是"清晰"线**：语音通信领域的经典经验值，10dB 以上对话轻松
- **3dB 是"可懂"底线**：3dB 时声音只比噪声大一点点，第二人耳要很费力才能听清；低于这条线就当听不见 → `rejected_low_snr`

顺带：你的转写引擎（ASR）也是"第一人耳"，它比人还笨——人在 3dB 还能猜，ASR 在 3dB 直接乱码。所以**人耳觉得"勉强"的（3~10dB 橙色），ASR 已经废了**。

## 6. 全部串起来

```
RawNoiseTracker 一直测房间底噪（比如 -60dBFS）
        │ 每段录音结束
        ▼
算这段的 rawRMS（比如 -40dBFS）
        │
SNR = 20×log10(0.01/0.001) = 20dB  →  绿色 ok
        │
如果这段其实很轻（-70dBFS）：SNR 看着还行但绝对响度太低
        → rejected_low_level（响度 < 0.0005 那条规则）
```

两个标签的区别：
- **rejected_low_snr**：声音响，但被噪声埋住了（环境吵）
- **rejected_low_level**：环境很安静，但声音本身太小声（离麦远/轻声说话）

一句话总结：**RMS 回答"多大"，底噪回答"房间多吵"，SNR 回答"这段声音够不够突出，值不值得转写"**。颜色分型就是把这第三个答案映射成人眼秒懂的三档。